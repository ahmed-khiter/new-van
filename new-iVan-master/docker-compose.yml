version: "3.9"

services:
  nextapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:5057"
    restart: always
    user: "111:111"
    env_file:
      - .env
    environment:
      NODE_ENV: production


